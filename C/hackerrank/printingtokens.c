#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){

/*
       char ch,*s;
       s = malloc(1024 * sizeof(char));
       s = realloc(ch, strlen(s)+1 );
       while((ch=getchar())!='\n')
       {
              putchar(ch);
              if(ch==' ')
              {
                     printf("\n");
              }
       }
       return 0;
*/
/*
       char ch,*s;
       s = malloc(1024 * sizeof(char));
       gets(s);
       s = realloc(s, strlen(s)+1 );
       for (char *c = s; *c != NULL; c++) {
              if (*c == ' ') {
                  *c = '\n';
              }
       }
       printf("%s", s);
       return 0;
*/
}